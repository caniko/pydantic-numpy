# Generated by simit. Manual edits will be reported as ci=drift.
name: Publish to PyPI

on:
  push:
    tags: ["[0-9]*"]

concurrency:
  group: ${{ github.workflow }}-${{ github.event_name }}-${{ github.head_ref || github.ref_name }}
  cancel-in-progress: true

jobs:
  publish:
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      contents: read
      id-token: write
    env:
      NIX_CONFIG: "experimental-features = nix-command flakes"
      XDG_CACHE_HOME: "/tmp/.cache"
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1

      - name: Install Nix
        uses: cachix/install-nix-action@630ae543ea3a38a9a4166f03376c02c50f408342 # v31

      - name: Validate release tag
        run: |
          set -euo pipefail
          tag="${GITHUB_REF_NAME:-${FORGE_REF_NAME:-${CODEBERG_REF_NAME:-}}}"
          if ! printf '%s\n' "$tag" | grep -Eq '^[0-9]+\.[0-9]+\.[0-9]+$'; then
            echo "Tag must be an exact semver version like 0.1.1, got '$tag'" >&2
            exit 1
          fi
          version="$(nix eval --raw --impure --expr '(builtins.fromTOML (builtins.readFile ./pyproject.toml)).project.version')"
          if [ "$tag" != "$version" ]; then
            echo "Tag $tag does not match project version $version" >&2
            exit 1
          fi

      - name: Project setup
        run: nix develop -c uv run --with 'numpy==2.3.5' --group test pytest tests

      - name: Build Nix package
        run: nix build .# --no-link

      - name: Publish to PyPI
        run: |
          nix develop -c uv build
          nix develop -c uv publish --trusted-publishing always
